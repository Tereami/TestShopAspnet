@model TestShopAspnet.ViewModels.CatalogViewModel

@{
    ViewBag.Title = "Каталог товаров";
}

<section>
    <div class="container">
        <div class="row">
            <div class="col-sm-3">
                <partial name="Partial/_Sidebar" />
            </div>

            <div class="col-sm-9 padding-right">
                <partial name="Partial/_Features" model="Model.Products"/>
            </div>
        </div>
    </div>
</section>

